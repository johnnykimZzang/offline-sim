# 2026-04-08

## 작업 1 — Phase 1: 단일 파일 → 모듈 분할

### 변경 목적
1178줄 단일 App.jsx의 유지보수 부담 해소. 향후 Phase 2~4 기능 추가 기반 마련.

### 수정 파일 (신규 25개)
- `src/App.jsx` (전면 재작성, 100줄)
- `src/design/tokens.js`
- `src/lib/{constants,format,simulator}.js`
- `src/state/{schema,reducer,useSimulator}.js`
- `src/insights/rules.js`
- `src/components/primitives/*` (6개)
- `src/components/{Header,ControlPanel,KpiGrid,InsightPanel}.jsx`
- `src/components/views/*` (6개)

### 문서 반영
- `docs/architecture.md` 신규
- `docs/decisions.md` 신규

### 검증
- `npm run build` 통과 (115ms, 40 모듈)
- 동작 회귀 없음 (수동 확인 대기)

---

## 작업 2 — Phase 2: 영속화 + 프리셋

### 변경 목적
매번 슬라이더 다시 맞추는 마찰 제거. 시나리오 단위 의사결정 지원.

### 수정 파일
- `src/state/useSimulator.js` — localStorage debounced persist + applyPreset 액션
- `src/state/presets.js` — 신규 (보수/현실/낙관/목표 4개)
- `src/components/PresetBar.jsx` — 신규 (시나리오 칩 + 전체 초기화)
- `src/components/primitives/Slider.jsx` — modified indicator 추가
- `src/components/primitives/Section.jsx` — reset button 추가
- `src/components/ControlPanel.jsx` — defaultValue + resetDomain wiring
- `src/App.jsx` — PresetBar wiring + hasModifications 계산

### 문서 반영
- `docs/architecture.md` (Persistence 섹션 추가)
- `docs/decisions.md` (Phase 2 결정 기록)
- `docs/changelog.md` (사용자 관점 변경 추가)

### 검증
- `npm run build` 통과 (106ms, 42 모듈)

### 남은 이슈
- localStorage 키는 `offline-sim-state-v1` — schema breaking change 시 v2로 올려야 함
- 프리셋 적용 시 어떤 도메인이 바뀌었는지 시각적 피드백 없음 (Phase 3에서 diff 표시 추가 검토)
- "현재 프리셋이 무엇인가" 표시 없음 — 의도적 단순화 (사용자가 자유롭게 편집 시작하면 어차피 무의미)

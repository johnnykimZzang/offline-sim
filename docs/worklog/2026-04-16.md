# 2026-04-16 작업 로그

## 작업 제목
고객 수 지표 추가 — OpsStrip, 보완 파라미터 3종, 툴팁 수정

## 변경 목적
대시보드에 일 매장 유입 고객 수 / 시간당 체류 고객 수를 노출. 기존 지표만으로 계산 가능하나, 영업시간(하드코딩) / 피크 집중도 / 재방문율 3개가 누락되어 함께 추가.

## 수정 파일

| 파일 | 변경 내용 |
|------|----------|
| `src/state/schema.js` | `fitting.peakTimeShare`, `crm.returnVisitRate`, `operations.operatingHours` 추가 |
| `src/lib/simulator.js` | `computeStaffing`·`computeFittingPath`에 `operatingHours` 반영; `simulateMonth` 반환에 파라미터 포함; `computeSummary`에 파생 지표 4개 추가 |
| `src/components/ControlPanel.jsx` | 슬라이더 3개 추가 (operatingHours, peakTimeShare, returnVisitRate) |
| `src/components/primitives/Slider.jsx` | 툴팁 `createPortal` 전환, `opacity: 1`, 좌우 경계 클램프 |
| `src/components/OpsStrip.jsx` | 신규 생성 — 운영 현황 스트립 5개 지표 |
| `src/App.jsx` | OpsStrip import + KpiGrid 하단에 삽입 |

## 문서 반영 파일
- `docs/frontend.md` — 화면 구조 업데이트, 컴포넌트 목록, 툴팁 구현 방식 추가
- `docs/changelog.md` — 사용자 관점 변경 요약 추가
- `docs/decisions.md` — 파생 지표 설계 판단, 툴팁 portal 결정 추가

## 남은 이슈 / TODO
- `returnVisitRate`가 현재 표시 전용 — 재방문 고객의 전환율 별도 적용 여부는 추후 판단
- `peakTimeShare`가 민감도 분석(sensitivity.js) 대상에 미포함 — 필요 시 추가
- `operatingHours`가 민감도 분석 대상에 미포함
